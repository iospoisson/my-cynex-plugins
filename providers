function getStreams(tmdbId, mediaType, season, episode) {
  // This must be SYNC and return an array directly.
  console.log('TestPlugin getStreams called', tmdbId, mediaType, season, episode);

  // Replace URL below with any direct MP4/M3U8 you know plays in your player
  return [{
    title: 'Demo Stream (TestPlugin)',
    name: 'Demo 720p',
    url: 'https://example.com/demo-stream.mp4', // TODO: put a real test URL here
    quality: '720p',
    language: 'en',
    provider: 'TestPlugin'
  }];
}
